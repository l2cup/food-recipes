<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="raf.rs.ispit.uros_nikolic_rn2117.foodrecipes">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="raf.rs.ispit.uros_nikolic_rn2117.foodrecipes.app.FoodRecipesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="raf.rs.ispit.uros_nikolic_rn2117.foodrecipes.presentation.view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.activity.MainActivity" />
        <activity android:name=".presentation.view.activity.DetailsActivity" />
    </application>

</manifest>